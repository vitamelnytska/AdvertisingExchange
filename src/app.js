const a = 1;

function foo(name) {
  const lastName = name;
  return lastName;
}

foo('Denis');
const x = 200;

console.log(x, a);

const arrow = (y) => y + 1;
arrow(2);

const obj = {
  name: 'Denis',
};

const name = obj.name;